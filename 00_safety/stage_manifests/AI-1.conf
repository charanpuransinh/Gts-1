# GTS Model Verification Manifest — AI-1
#
# Consumed by 00_safety/MODEL_GATE.sh. Values are derived from the existing,
# already-verified AI-1 file registry (29_docs/file_registry.md) and the
# AI-1 HARD_GATE.sh test wiring — not invented.
#
# REQUIRES: space-separated list of prerequisite stage names (their manifests
#           must exist and their MODEL_PASS.lock must be present). Empty for
#           a root/first stage.

REQUIRES=""

UNIT_TEST_DIRS="01_market_data/tests"
INTEGRATION_TEST_DIRS="28_tests"

COMPILE_DIRS="01_market_data 23_database 24_api 27_system 28_tests"

# Space-separated tokens. Any directory/file path under COMPILE_DIRS matching
# one of these (case-insensitive) fails the scope check.
FORBIDDEN_STAGE_TOKENS="ai-2 ai-3 ai-4"
FORBIDDEN_FILENAMES="strategy_engine.py signal_engine.py risk_engine.py execution_engine.py broker_client.py"
